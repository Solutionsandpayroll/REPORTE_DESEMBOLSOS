from django.apps import AppConfig

class GeneradorPlanoConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'generador_plano'
    verbose_name = 'Generador de Planos Bancarios'
